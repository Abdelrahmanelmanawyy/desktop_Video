# FFmpeg binaries (Windows)

Place the Windows FFmpeg executables here:

- **ffmpeg.exe**
- **ffprobe.exe**

Download from: https://www.gyan.dev/ffmpeg/builds/ (e.g. `ffmpeg-release-essentials.zip`), then copy `bin/ffmpeg.exe` and `bin/ffprobe.exe` into this folder.
